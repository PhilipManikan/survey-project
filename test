          {this.state.dataLoaded == true && this.state.survey[0].feedbackQuestion.map((list, index) => {
                                return (
                                    <tr key={index}>
                                        <td>
                                            {list.number}.{list.question}{list.type}
                                        </td>
                                    </tr>
                                )
                            })}